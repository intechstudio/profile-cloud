rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
   // Match any document in the 'cities' collection
    match /profiles/{profile} {
      allow read: if resource.data.public == 'public';
      allow write: if request.auth != null;
    }
  }
}